server:
  port: 9003

spring:
  application:
    name: empl-client

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


feign:
  hystrix:
    enabled: true # Turn on Feign's fuse function